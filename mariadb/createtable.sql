CREATE TABLE persons (
person_id integer primary key auto_increment, 
name varchar(30), 
address varchar(50), 
phone varchar(30), 
birthyear integer, 
info1 varchar(255), 
info2 varchar(255), 
info3 varchar(255)
);